import XCTest
@testable import RingerInteractive

final class RingerInteractiveTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RingerInteractive().text, "Hello, World!")
    }
}
